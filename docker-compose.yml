services:
  postgres:
    image: postgres:16-alpine
    container_name: canchas_postgres
    environment:
      POSTGRES_DB: canchas_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: adminpassword
    ports:
      - 5433:5432
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: